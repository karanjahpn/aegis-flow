# AEGIS-FLOW v1.0.0 — Python Dependencies

# Core
psutil>=5.9.0          # System & process metrics
cryptography>=41.0.0   # Cryptographic primitives (future: key management)

# Blockchain (optional — install only if using --blockchain flag)
web3>=6.0.0            # Ethereum interaction

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0

# Linting (dev)
flake8>=6.1.0
